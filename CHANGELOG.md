## 2026-04-10 — Полосковый индикатор контроля ЦАРГИ

### index_landscape_knob.html — вертикальная шкала с зумом диапазона BASE/HYST

**Задача**: риска teloEnd на кольце ЦАРГА в gauge была неинформативна — при реальной работе расстояние от текущей температуры до порога залёта ~0.2°C, и на шкале 0-105°C риска сливается с полоской текущего значения. Нужен визуально различимый индикатор приближения к порогам DELTA (снижение скорости) и HYST (завершение отбора).

**Решение**: новый блок «Контроль ЦАРГИ» — вертикальная полоска с динамическим диапазоном ~0.6°C (base-0.2 .. hyst+0.2), расположенная между диаграммой и правой колонкой.

1. **Layout**: grid 3→4 колонки (`minmax(140px,1.2fr)` для нового блока), `min-width: 680px`.
2. **Canvas `drawTsarBar()`**: рисует полоску с градиентом (cyan→green→amber→red), риски BASE/DELTA/HYST, пунктирную линию текущей температуры, шкалу с шагом 0.05°C, значения с точностью 0.01°C. Подсветка зоны DELTA-HYST.
3. **До этапа ТЕЛО**: индикатор обесцвечен (saturation 0.25) — видны серые контуры, но не отвлекает. BASE = текущая tsar, DELTA/HYST с дефолтными смещениями.
4. **При ТЕЛО/NASEBYA**: полная цветная версия — BASE=rtsarM, DELTA=rtsarM+delta+pressCorr, HYST=rtsarM+hyst+pressCorr. Обновляется real-time с учётом давления.
5. **Под полоской**: текущее значение ЦАРГИ, цвет меняется на amber при достижении DELTA и red при HYST.
6. **Убрана риска teloEnd с gauge** — заменена этим индикатором.

**Изменённые файлы**:
- `index_landscape_knob.html`: CSS grid 4 col, CSS tsar-bar-panel, HTML tsar-bar panel, JS drawTsarBar(), убран teloEnd marker из gauge

---

## 2026-04-10 — Фикс: конверсия единиц давления в расчёте teloEnd

### index_landscape_knob.html — исправлена формула расчёта порога окончания ТЕЛО

**Задача**: риска на кольце ЦАРГА показывала неверную позицию и неверное значение рядом с ней.

**Решение**: в исходной формуле `pressure` с ESP32 приходит в мм.рт.ст. (`sensors.pressure * 0.75`), а `adPressM` — в сырых гПа (как хранится внутри ProcessEngine). Разница давлений считалась напрямую между разными единицами. Исправлено: `adPressM × 0.75` → перевод в мм.рт.ст., разница `/ 0.75` → обратно в гПа для умножения на `pressureCoeff` (0.028 °C/гПа). Теперь формула полностью эквивалентна C++: `rtsarM + histeresis + ((pressure_mmHg - adPressM_mmHg) / 0.75) × pressureCoeff`.

**Изменённые файлы**:
- `index_landscape_knob.html`: конверсия adPressM гПа→мм.рт.ст., пересчёт разницы в гПа для pressureCoeff

---

## 2026-04-10 — Мониторинг: сохранение объёмов голов/тела после финиша

### index_landscape_knob.html — данные ГОЛОВЫ/ТЕЛО остаются видимыми при FINISHWORK

**Задача**: при переходе системы в FINISHWORK данные по отобранным объёмам голов и тела в блоках ГОЛОВЫ/ТЕЛО пропадали (затирались `--`). Нужно чтобы они оставались до выключения системы или запуска нового процесса.

**Решение**:
- Добавлены переменные `preservedHeadsVolDone`, `preservedBodyVolDone`, `preservedHeadsTotalTarget`, `preservedAsVolume` — сохраняют последние ненулевые значения при каждом poll.
- В `else`-ветках `updateCalculationsHUD()` (когда stage не GOLOVY/TELO) вместо `--` выводятся сохранённые значения.
- При старте нового процесса (`stage === NAGREV` или `NAGREV_DEFL`) все сохранённые значения обнуляются.

**Изменённые файлы**:
- `index_landscape_knob.html`: preserved-переменные, логика сохранения/очистки в syncData(), обновлённые else-ветки в updateCalculationsHUD()

---

## 2026-04-10 — Царга: риска расчётной температуры окончания ТЕЛО (real-time)

### index_landscape_knob.html — динамический маркер порога окончания отбора на кольце ЦАРГА

**Задача**: добавить на кольцо ЦАРГА риску, показывающую температуру при которой завершится отбор ТЕЛО (Шпора — финиш, Стандарт — повторная стабилизация). Значение рассчитывается в реальном времени с учётом коррекции по давлению.

**Решение**:
- **Формула** (полная аналогия C++ ProcessEngine): `rtsarM + histeresis + (текущее давление − adPressM) × pressureCoeff`
- **Скрытие до начала ТЕЛО**: `show: teloEnd > 0 && teloEnd < GS.maxT`. Пока `rtsarM ≤ 0.1` (этап ТЕЛО не начался) — риски нет на диаграмме.
- **Обновление в реальном времени**: `pressureCorrection` пересчитывается каждую секунду при отрисовке диаграммы, риска плавно сдвигается при изменении давления.
- **Цвет** `#00ff88` (как кольцо царги), **подпись** — значение с 1 знаком после запятой (например `78.5`).

**Изменённые файлы**:
- `index_landscape_knob.html`: markers array (+teloEnd маркер), расчёт порога в drawGauge()

** Не удачное решение**
Нужно переделать Проверил, получается не информативно, поскольку в реальной работе обычно расстояние от текущей температуры и температуры залета отличается на 2 с небольшим десятых градуса, и получается риска на дуге царги вплотную к показаниям полоски на дуге. Надо придумать что то другое, давай подумаем как можно визуально отслеживать подход температуры в царге к показаниям для снижения скорости отбора и окончании отбора в методе шпора и окончании отбора в методе стандарт для отбора тело.

---

## 2026-04-10 — Кольцо ВОДА: риска 50°C и предупреждение перегрева

### index_landscape_knob.html — серая риска на 50°C и красное кольцо при превышении

**Задача**: добавить на кольцо ВОДА визуальный маркер 50°C и индикацию перегрева — кольцо становится красным при температуре выше 50°C, без уведомлений/аварий, просто смена цвета.

**Решение**:
1. **Серая риска на 50°C**: добавлен маркер `{val:50, label:'50', color:'#666666', ringKey:'aqua', show:true}` в массив markers диаграммы. Риска пересекает кольцо ВОДА (как маркеры TSA/СТОП/TARA на своих кольцах), цвет серый `#666666`, подпись «50» у наружного края.
2. **Красное кольцо при t > 50°C**: добавлено условие `if(ring.key==='aqua'&&temp>50) {rc='#ff3355'; rb='rgba(255,51,85,0.15)';}` в цикле отрисовки колец (аналог TSA-лимита). При снижении температуры ниже 50°C кольцо автоматически возвращается к обычному цвету `#00f0ff`.

**Изменённые файлы**:
- `index_landscape_knob.html`: markers array (+aqua 50°C), ring color override для aqua > 50°C

---

## 2026-04-10 — Компоновка index_landscape_knob.html

### index_landscape_knob.html — 4 правки по результатам тестирования

**Задача**: корректировки компоновки, точности данных и стиля по результатам проверки на ESP32.

**Решение**:
1. **Power Knob поднят к ВОДА**: убраны padding/margin контейнера потенциометра (`padding:4px 0 2px` → `margin:0;padding:0`), ручка максимально близко к значению ВОДА. Убран `margin-top:auto` у блока МОЩН/BOX/ВЛАЖН — он расталкивал контент вниз панели, создавая пустое место между потенциометром и МОЩН. Padding блока уменьшен `10px→6px`.
2. **Точность датчиков на Мониторинге — 2 знака**: функция `setTemp()` на странице Мониторинг выводила `toFixed(1)`, в то время как на страницах Дистилляция/Ректификация и на LCD — `toFixed(2)`. Исправлено: `toFixed(1)` → `toFixed(2)` для единообразия.
3. **Навигационные кнопки прижаты к контенту**: grid-layout `.hud-container` имел `grid-template-rows: 44px 1fr 36px` — навбар был привязан к нижнему краю экрана. Первая правка (`36px→auto`) не помогла, т.к. `1fr` всё равно растягивал main на весь экран. Финально изменено на `44px auto auto` — кнопки идут сразу после контента.
4. **Шкала диаграммы в стиле PowerKnob**:
   - Цвета: крупные деления (10°) и подписи — `#ff8c00` (amber), средние (5°) и мелкие (1°) — `#00f0ff` (cyan). Шрифт подписей 8→9px.
   - Убраны фоновое свечение и декоративный контур за шкалой (были `rgba(0,240,255,0.08)` и `0.12`).
   - Направление рисок: из наружу→внутрь (как у PowerKnob), подписи перенесены на наружный край.
   - Длины рисок: крупные 12px, средние 12px (было 8px), мелкие 6px (было 3px = половина от крупных).
   - Фоновые дуги колец температур оставлены без изменений.

**Изменённые файлы**:
- `index_landscape_knob.html`: CSS grid-rows (1fr→auto), setTemp toFixed(2), PowerKnob padding, убран margin-top:auto, gauge scale colors

---

## 2026-04-09 — Power Knob: SVG-потенциометр для отображения мощности

### power_knob.html + power_knob.js + index_landscape_knob.html — визуальная ручка-потенциометр

**Задача**: создать визуальный элемент-потенциометр (как на физических приборах) для отображения заданной мощности в блоке Температуры на странице Мониторинг. Оригинальный `index_landscape.html` не трогать — работа в отдельном файле.

**Решение**:

1. **Стенд-alone компонент `power_knob.html`** — автономная страница для отладки:
   - SVG ручка-потенциометр в стиле Sci-Fi HUD (металлический 3D, knurling 60 насечек, втулка, белый указатель)
   - Дуга 270°, диапазон 100–7000W, деления каждые 100W
   - Подписи 0–7 (кВт), нулевая риска
   - Мелкие риски #00f0ff (cyan), тысячные риски и подписи #ff8c00 (amber)
   - Шкала близко к ручке (радиус делений 68px, подписи 82px)
   - JS-функция `setPower(watts)` для обновления указателя
   - Плавная CSS-анимация поворота 0.6s cubic-bezier

2. **Модульная версия `power_knob.js`** — IIFE-компонент `PowerKnob` для встраивания:
   - API: `PowerKnob.init(containerId)`, `PowerKnob.update(watts)`, `PowerKnob.initialized`
   - Автоматическое создание SVG внутри контейнера, все ID с префиксом `pk` для изоляции

3. **Интеграция `index_landscape_knob.html`** — копия `index_landscape.html` с потенциометром:
   - Потенциометр размещён в блоке Температуры: ниже датчика ВОДА, выше строки МОЩН
   - Код PowerKnob встроен инлайн (без внешних зависимостей, работает автономно)
   - Инициализация при загрузке страницы (дефолт 3000W, виден без ESP32)
   - Автообновление указателя при получении `cfg.power` от ESP32 и при изменении через +/- кнопки

**Итерации разработки** (5 версий по обратной связи):
- v1: базовый потенциометр с кнопками и отображением значения → убраны кнопки и значение
- v2: реалистичная ручка с knurling, шкала снаружи → радиус шкалы уменьшен
- v3: шкала ближе к ручке (68px), убрано отображение значения
- v4: фикс поворота указателя (+90°), нулевая риска, подписи без 'k'
- v5: подписи 1–7 (без нулей), яркие цвета #ff8c00/#00f0ff

**Изменённые файлы**:
- `power_knob.html`: стенд-alone компонент (новый)
- `power_knob.js`: модульная версия для встраивания (новый)
- `index_landscape_knob.html`: копия index_landscape.html с интегрированным потенциометром (новый)
- `index_landscape.html`: **НЕ изменён**

---

## 2026-04-09 — Очистка репозитория от системных файлов

### .gitignore — убрано 410 файлов из трекинга

**Задача**: в репозитории появились системные папки `skills/`, `.env`, submodule-папки `BuhloWar/`, `repo/` и изменённые `.md` файлы от автосинхронизации.

**Решение**:
- Создан `.gitignore`: `skills/`, `.env`, `BuhloWar/`, `repo/`
- Удалено из git tracking: `git rm --cached -r` (410 файлов)
- Оригинальные рабочие файлы не затронуты

**Изменённые файлы**:
- `.gitignore`: создан (новый)

---

## 2026-04-09 — Фикс: minOpenTime ограничивался 1000 при загрузке из API

### index_landscape.html — старые хардкоды при загрузке конфига не совпадали с cfgRanges

**Задача**: minOpenTime невозможно задать больше 1000 в WEB, хотя cfgRanges разрешает до 2000.

**Решение**: при загрузке конфига из API (функция обновления UI) были найдены старые хардкодные проверки диапазонов, которые не были обновлены при создании cfgRanges:
- `minOpenTime`: `>=10 && <=1000` → `>=50 && <=2000` (сбрасывал значение >1000 на дефолт 100)
- `speedHeadCorr`: `>=0 && <=200` → `>=10 && <=200` (позволял 0, хотя cfgRanges min=10)
- `speedBodyCorr`: `>=0 && <=200` → `>=10 && <=200` (аналогично)

**Изменённые файлы**:
- `index_landscape.html`: строки валидации при загрузке конфига приведены к cfgRanges

---

## 2026-04-08 — Диапазоны параметров WEB интерфейса + синхронизация с LCD

### index_landscape.html + menu_settings.h — cfgRanges для всех 21 параметра

**Задача**: провести аудит и установить диапазоны (min/max) для ВСЕХ параметров в WEB интерфейсе и LCD меню, чтобы исключить ввод некорректных значений. Диапазоны должны быть общими для LCD и WEB.

**Решение**:
1. **Инфраструктура**: создан объект `cfgRanges` в WEB (аналог `engRanges` для инженерного меню). Функция `adjCfg()` переписана на использование `cfgRanges` вместо хардкодных проверок. Также добавлен массив `cfgAutoSave` — параметры с автосохранением при изменении (расширен с 3 до 9 полей).
2. **Общие настройки (10 параметров)** — диапазоны установлены, LCD синхронизирован:
   - emergencyTime: min=1, max=**10** (было max=5 в LCD)
   - nasebTime: min=1, max=30 (=LCD)
   - reklapTime: min=1, max=30 (=LCD)
   - boxMaxTemp: min=50, max=70 (=LCD)
   - power: min=100, max=**7000** (было max=3500)
   - asVolume: min=1000, max=10000 (=LCD)
   - speedHeadCorr: min=**10**, max=200 (было min=0 в WEB)
   - speedBodyCorr: min=**10**, max=200 (было min=0 в WEB)
   - minOpenTime: min=50, max=**2000** (было max=500 в LCD, max=1000 в WEB)
   - chekwifi: min=1, max=5 (=LCD)
3. **Дистилляция (6 параметров)** — диапазоны установлены (в LCD этих параметров нет, они настраиваются в меню процесса):
   - razgonTemp: min=40, max=99
   - bakStopTemp: min=50, max=99
   - midterm: min=0, max=98 (связан с midterm_abv через температурно-крепостную зависимость)
   - midterm_abv: min=0, max=98 (связан с midterm через getOutputABVForTemp/getTempForOutputABV)
   - mixerOnTime: min=5, max=300
   - mixerOffTime: min=1, max=120
4. **Ректификация (5 параметров)** — диапазоны установлены:
   - tsaLimit: min=40, max=55 (=LCD)
   - cycleLim: min=1, max=**5** (было без лимитов в WEB)
   - histeresis: min=0.06, max=1.00
   - delta: min=0.06, max=1.00

**Изменённые файлы**:
- `index_landscape.html`: добавлен cfgRanges (21 параметр), cfgAutoSave, adjCfg() переписана
- `menu_settings.h`: emergencyTime max 5→10, power max 3500→7000, minOpenTime max 500→2000

---

## 2026-04-08 — Доработки инженерного меню WEB интерфейса

### index_landscape.html — переименования + перенос кнопки + фикс бага shporaStabMs

**Задача**: доработать инженерное меню по результатам тестирования — переименования, компактность, исправление критического бага с единицами измерения.

**Решение**:
1. **Кнопка ⚙ перенесена** в строку заголовка страницы «Настройки» (у правого края). Старый блок «ИНЖЕНЕРНОЕ МЕНЮ» с подписью и кнопкой внизу страницы удалён — экономия высоты.
2. **Переименования подписей** (только HTML label, логика и JSON ключи не затронуты):
   - СКОРОСТИ ЭКСТРАКЦИИ → СКОРОСТИ ОТБОРА
   - ДО ОТБОРА → ДООТБОР ГОЛОВЫ
   - КОЭФФ. → КОЭФФ.СНИЖЕНИЯ ОТБОРА
   - СТАБИЛИЗАЦИЯ → СТАБИЛИЗАЦИЯ (до след.снижения)
   - МИН. ТЕЛО → МИН.СКОРОСТЬ ТЕЛО
   - ДОЛИ ОТБОРА → ДОЛИ ОТБОРА ГОЛОВ
   - КОРРЕКЦИЯ → КОЭФФ.КОРРЕКЦИИ ПО АД
   - ВРЕМЕНА ПРОЦЕССОВ → ВРЕМЯ ЭТАПОВ
   - ОХЛАЖДЕНИЕ → ОСТАНОВ ОХЛАЖДЕНИЯ
   - КАЛИБР. СУХОЙ → ПРОЛИВ
3. **Критический баг: shporaStabMs мс↔сек** — C++ хранит `shporaStabMs = 60000` (мс), но UI показывала raw-значение "60000" вместо "60 сек". Диапазон UI `[10,300]` приводил к скачку на 300 при редактировании, а сохранённое значение `300` интерпретировалось как 300 мс = 0.3 сек (вместо 300 сек). Фикс:
   - Добавлен массив `engMsFields = ['shporaStabMs']`
   - `openEngMenu()`: делит на 1000 при загрузке (60000 → 60)
   - `saveEngMenu()`: умножает на 1000 при сохранении (60 → 60000)
   - Убраны мёртвые параметры `displayMul` из `adjEng()` вызовов
4. **Проверка всех остальных параметров** — coolingDurationSec, bakstopDelaySec, calibDrySec, calibCapacitySec хранятся и используются в секундах, конвертация не нужна, багов нет. Доли (headsShare*) корректно конвертируются %↔доля через engPctFields.

**Изменённые файлы**:
- `index_landscape.html`: HTML подписи, кнопка инженерного меню, JS конвертация shporaStabMs

---

## 2026-04-08 — План: Инженерное меню + фикс NTP таймзоны

### Анализ хардкод-констант (~70 найдено)

**Задача**: вынести настраиваемые константы из C++ кода в Engineering Menu (WEB интерфейс), чтобы пользователь мог менять их без перекомпиляции. Параллельно — исправить баг NTP таймзоны и провести аудит диапазонов существующих настроек.

**Решение — согласованный план (пункты 1-6 + фикс NTP):**

1. **Базовые скорости экстракции** (ProcessEngine.cpp:1004-1005, 1499)
   - Головы: 50 мл/ч/кВт (distillation heads: 50 мл/ч)
   - Тело: 500 мл/ч/кВт
   - Перегон голов: 50 мл/ч

2. **Параметры Шпоры** (ProcessEngine.cpp:5-6)
   - CORRELATION_COEFF: диапазон 1.05–1.50, шаг 0.01, по умолч. 1.13
   - SHPORA_STABILIZATION: диапазон 10–300 сек, шаг 10, по умолч. 60

3. **MIN_BODY_SPEED** (ProcessEngine.cpp:1428)
   - Диапазон 100–800 мл/ч, шаг 50, по умолч. 300

4. **Доли отбора** (ProcessEngine.cpp:1098, 1148, 1195, 1238)
   - Стандарт головы: 5–20%, шаг 1, по умолч. 10
   - КСС спит: 1–5%, шаг 0.5, по умолч. 2
   - КСС стандарт: 1–10%, шаг 0.5, по умолч. 3
   - КСС акатело: 5–30%, шаг 1, по умолч. 15

5. **Коррекция давления** (ProcessEngine.cpp:919, 1370)
   - Коэффициент: 0.020–0.040, шаг 0.001, по умолч. 0.028 (теоретич. ~0.03°С/мм)
   - Дельта ЦАРГ: 2.0–15.0°С, шаг 0.5, по умолч. 5.0

6. **Времена процессов** (ProcessEngine.cpp:385, 393, 878, 1568; OutputManager.h:98)
   - Охлаждение: 60–600 сек, шаг 30, по умолч. 300
   - Safe shutdown: 60–600 сек, шаг 30, по умолч. 300
   - Бакстоп задержка: 0–30 сек, шаг 1, по умолч. 5
   - Калибровка сухой тест: 5–30 сек, шаг 5, по умолч. 10
   - Калибровка capacity: 30–120 сек, шаг 10, по умолч. 60

7. **Предельные температуры** — НЕ трогать (ABSOLUTE_MAX_TEMP, ABSOLUTE_MIN_TEMP в config.h закомментированы)

8. **Баг NTP таймзоны** (AppNetwork.cpp:~857)
   - `syncNTP()` хардкодит `3 * 3600` вместо чтения `configManager.getConfig().timezoneOffset`
   - Фикс: заменить `configTime(3 * 3600, ...)` на `configTime(tzOffset * 3600, ...)`
   - Также: дублирование `timezoneOffset` в SystemConfig (preferences.h) и GlobalConfig (ProcessCommon.h)

**UI Инженерного меню:**
- Кнопка с иконкой wrench в странице Настройки
- Всплывающее окно «ИНЖЕНЕРНОЕ МЕНЮ»
- Подтверждение при открытии: «Вы уверены? Эти параметры влияют на процесс»
- Все параметры с диапазонами, шагами и значениями по умолчанию
- Кнопка СОХРАНИТЬ внутри диалога

**Дополнительно:**
- Аудит диапазонов/шагов в существующих настройках интерфейса
- Давление 0.0278 дублируется в двух местах (строки 919 и 1370) — объединить

**Изменённые файлы** (план):
- `ProcessEngine.cpp`: замена хардкодов на cfg.поля
- `OutputManager.h`: SAFE_SHUTDOWN_DURATION → cfg
- `preferences.h` / `preferences.cpp`: новые поля в SystemConfig
- `AppNetwork.cpp`: фикс NTP таймзоны
- `index_landscape.html`: инженерное меню (UI)
- `AI_RULES.md`: новое правило GitHub-сверки

---

## 2026-04-07 — Последние штрихи интерфейса

### index_landscape.html — подписи клапанов и унификация кнопок

**Задача**: мелкие правки перед тестированием на системе.

**Решение**:
1. **Подписи клапанов в ректификации**: КЛ.ГОЛОВ → ИСП.КЛ.ГОЛОВ, КЛ.ТЕЛО → ТИП.КЛ.ТЕЛО.
2. **Унификация кнопок**: добавлен CSS класс `.action-btn` (padding:10px, font-size:12px, letter-spacing:0.15em) — такой же размер как `.save-btn` в Дистилляции. Цветные варианты: `.amber`, `.green`, `.blue`, `.purple`. Добавлен `.action-btn-grid` для сетки 2 колонки.
3. **Окно Настройки**: все кнопки (СОХРАНИТЬ, ТЕСТ ЗВУКА, СОХР. ПРОФИЛЬ, ЗАГР. ПРОФИЛЬ, ИНСТРУКЦИЯ, ЛОГИ) переведены на `.action-btn` класс. Сгруппированы в сетки 2×2 для компактности: СОХРАНИТЬ+ТЕСТ ЗВУКА, СОХР.ПРОФИЛЬ+ЗАГР.ПРОФИЛЬ, ИНСТРУКЦИЯ+ЛОГИ.

**Изменённые файлы**:
- `index_landscape.html`: CSS классы, подписи клапанов, кнопки Настроек

---

## 2026-04-07 — Сессия N

### index_landscape.html — UI доработки нового ландшафтного интерфейса

**Задача**: мелкие замечания к Sci-Fi HUD интерфейсу.

**Решение**:
1. **Шапка**: убрана кнопка ⚙ Настройки (есть внизу в навигации), убраны индикаторы `760 mm` и `3000 W` (дублируют барометр и блок МОЩН), аббревиатуры заменены на полные названия: НГР→НАГРЕВ, МШЛ→МЕШАЛКА, ВДА→ВОДА. Убраны JS-обновления удалённых элементов.
2. **Заголовки блоков**: все заголовки сделаны цветными (Температуры=amber, Атмосфера=blue, Система=green, Головы=amber, Тело=green, Коррекция=purple, Настройки=blue, Калибровка=green, Облако=blue).
3. **Текст параметров ярче**: `.temp-label` #4a5568→#8a95a8, `.sys-label` #3a4a60→#6a7588, `.s-row label` #7a8598→#a0a8b8, `.calc-row-data .label` #4a5568→#8a95a8, `.temp-limit` #3a4558→#5a6577.
4. **Обводка блоков**: `--panel-border` rgba(0,240,255,0.12)→rgba(0,240,255,0.22). Border-bottom заголовков: rgba(0,0,0,0.03)→rgba(255,255,255,0.06).
5. **Мониторинг**: из блока Коррекция убраны КРЕП.КУБ и КРЕП.ОТБОР (дублируют блок Система). Удалены соответствующие HTML-элементы и JS-обновления.
6. **Тест звука**: кнопка перенесена из шапки в страницу Настройки (после блока WIFI ЧЕК).
7. **Анимация диаграммы**: добавлена 3-фазная анимация `animateGaugeEntry()` — раскрытие колец до макс→пауза→схлопывание к реальным значениям. Запускается при переключении на страницу Мониторинг. Барометр анимируется одновременно. Во время анимации обновление барометра заблокировано.

**Изменённые файлы**:
- `index_landscape.html`: все 7 пунктов

---

## 2026-04-05

### index.html — Мобильная адаптивность WEB интерфейса
`98459a2` (+ `a3ea466`, `7e6d590`, `35726ac`, `da336fd` — тестовые итерации через `index_mobile.html`)

- **Проблема**: WEB интерфейс не помещался на экране мобильного телефона (375px). Control Bar разбивался на 2 строки, gauge диаграмма обрезала содержимое справа, общая компоновка не влезала по ширине.
- **Итеративное решение** через отдельный тестовый файл `index_mobile.html` (4 раунда тестирования):
  1. Сначала `overflow-x-hidden` — обрезал контент справа (отменено).
  2. Уменьшение реальных размеров: gauge `w-40`, padding `p-3` — частично помогло.
  3. Уменьшение `gap` в Control Bar — не решило перенос на 2 строки.
  4. **Финальное решение**: замена `grid grid-cols-8` на `flex` + уменьшение кнопок/шрифта на мобилке.
- **Итоговые изменения в `index.html`**:
  - **Header**: `gap-6` → `gap-2 sm:gap-6` — иконки нагрева/мешалки/воды компактнее на мобилке.
  - **Top Control Bar**: `grid grid-cols-8` → `flex items-center justify-between` + кнопки `w-7 sm:w-8` + шрифт `text-[9px] sm:text-[10px]` + `flex-shrink-0` на всех элементах — всё помещается в одну строку на 375px.
  - **Gauge**: `w-52` → `w-40 sm:w-52`, canvas масштабируется через CSS `style="width:100%;height:100%"` с сохранением `width="208" height="208"` для корректного рисования.
  - **Карточка мониторинга**: `p-4` → `p-3 sm:p-4` — чуть компактнее на мобильных.
- Все изменения используют Tailwind responsive (`sm:` breakpoint 640px), на десктопе отображение не изменилось.

### index.html — Favicon подключён
`eaf57c0`

- Добавлен `<link rel="icon" type="image/svg+xml" href="favicon.svg">` в `<head>`.
- Favicon: SVG иконка vintage copper аламбик-дистиллятор.

### ProcessEngine.cpp + index.html — WEB мониторинг тела в Стандарт методе: отображение при NASEBYA и фикс фантомного объёма
`cc40528`

**Проблема 1: WEB не показывает данные тела при переходе "на себя"**
- При залёте в Стандарт методе система переходит в NASEBYA (стабилизация "на себя"). WEB мониторинг показывал `"--"` для метода, скорости, цикла, расч/факт.
- C++ `updateRectWebInfo()` обнулял `bodyMethodName`, `bodySpeed`, `bodyCycle` при любом этапе кроме TELO.
- JS `index.html` показывал блок тела только при `stage == 'TELO'`.
- **Фикс C++**: добавлена ветка `else if (currentStage == Stage::NASEBYA)` — при "на себя" данные тела сохраняются, не обнуляются.
- **Фикс JS**: три места в `index.html` изменены с `stage == 'TELO'` на `stage == 'TELO' || stage == 'NASEBYA'`. При NASEBYA рядом с циклом показывается пометка `⏳`.

**Проблема 2: Фантомный объём при возврате из NASEBYA**
- При возврате из NASEBYA в TELO первый расчёт `dt_h` охватывал всё время "на себя" (10-20 мин), и `bodyVolDone += скорость × время_NASEBYA` прибавлял объём за время когда клапан был закрыт.
- **Фикс**: `lastVolCalcTime = millis()` перед `changeStage(Stage::TELO)` в `handleNasebya()`. Отсчёт интервала начинается заново, `bodyVolDone` сохраняется и продолжает корректно накапливаться.

**Влияние на Шпору**: Шпора при залёте вызывает `finishTelo()` и никогда не переходит в NASEBYA. Ни одно изменение не затрагивает логику Шпоры.

### CloudManager.h — FreeRTOS задача: нулевая блокировка loop
`0ccf4ae`

- **Все HTTP-запросы вынесены в отдельную FreeRTOS задачу на core 0** (там WiFi/TCP стек). Основной loop на core 1 больше никогда не блокируется, независимо от состояния сервера.
- **Архитектура**: cloud задача (core 0, приоритет 0, стек 8KB) выполняет все HTTP-запросы, backoff, управление `_client`. Основной loop вызывает `update()` — который копирует телеметрию через mutex (~2 мкс), читает очереди команд/настроек, вызывает callbacks. Задержка — ноль.
- **Связь loop↔cloud**: телеметрия — mutex (запись из loop, чтение из cloud); команды и настройки — FreeRTOS очереди (cloud→loop, неблокирующее чтение).
- **Callbacks** `onCommand`/`onSettings` вызываются из контекста основного loop — безопасно, без race conditions.
- **Ресурсы**: ~16KB heap (8KB стек задачи + 7KB очереди + 1KB overhead).
- Публичный API идентичен — **никаких изменений в других файлах**.

### CloudManager.h — _client.stop() только при ошибках
`8d9caea`

- Предыдущая версия вызывала `_client.stop()` перед каждым запросом, что заставляло делать новый TLS handshake каждый раз (2-5 сек overhead). Web-интерфейс с SD карты начал притормаживать, система зависала.
- Теперь `_client.stop()` вызывается **только при `errorCount > 0`** (предыдущий запрос завершился ошибкой = мёртвое TCP соединение). При успехе соединение переиспользуется с нулевой задержкой.

### CloudManager.h — Предотвращение 3-минутного зависания loop
`c302901`

- **Причина**: при недоступности сервера TCP ретрансмиссии на переиспользуемом мёртвом соединении блокировали loop на 1→2→4→8→16 сек суммарно до 3 минут.
- **Решение**: `_client.stop()` перед `http.begin()` (закрывает мёртвое TCP), экспоненциальный backoff при ошибках (интервалы: 2→4→8→16→32 сек, макс. множитель x5), обновление lastCheckMs даже при ошибке.
- Добавлено логирование backoff: `[Cloud] Backoff: errorCount=N, next interval=X ms`.

### CloudManager.h + api.php — Fix ложного errorCount от settings check
`dabe4f6`

- `checkSettings()` возвращал `false` при `onSettings == nullptr` (callback никогда не подключался в проекте). Cloud задача считала это сетевой ошибкой → `errorCount = 1` каждую минуту.
- Исправлено: при `onSettings == nullptr` возвращается `true` (не ошибка, фича не используется). Ложный backoff убран.
- `api.php` GET settings не возвращал `settings_last_update` → ESP32 не могла определить новизну настроек → механизм cloud→device settings был мёртв.
- Исправлено: добавлено `settings_last_update` в ответ GET settings.

### Анализ: LOOP Slow 56-126 мс — не является проблемой
Исследование, без изменений кода

- `[LOOP] Slow: 56-126 ms` появляется ~2.5 раза/сек — это нормально для контроллера.
- Причина: `buildTelemetryJson()` вызывается каждый loop (~150 раз/сек), делает 60+ конкатенаций String + `logger.readNewLog()` открывает SD-файл. На слабых итерациях совпадает с обновлением DS18B20/BME280.
- 56-126 мс не влияет на работу: процессы идут с секундными интервалами, LCD раз в 500 мс.
- NetMgr, CloudManager и MEM check — выполняются в отдельных FreeRTOS задачах на core 0, не блокируют loop на core 1.
- Если потребуется ускорить — вызывать `buildTelemetryJson()` раз в 2 сек вместо каждого loop.

### AppNetwork.h/cpp + BuhloWar.ino — Fix Kernel Panic при загрузке от раннего HTTP запроса
`5041d94`

- **Причина**: NetworkTask запускается на core 0 до `processEngine.begin()` (строка 188 vs 271 в setup). Если браузер успевал сделать запрос в это окно (~1-2 сек), `processEngine->getSensorData()` обращался к `sensorAdapter = nullptr` → Core 0 panic (LoadProhibited).
- **Решение**: добавлен флаг `systemReady` в AppNetwork. Все вызовы `server->handleClient()` защищены проверкой `systemReady`. API handlers недоступны до полной инициализации системы.
- `appNetwork.setSystemReady(true)` вызывается в setup() после **всей** инициализации (строка 292) — после `processEngine.begin()`, создания всех меню и LCD.
- Защищены все точки вызова `handleClient()`: `update()`, `beginNetwork()`, `connectToWiFi()` (обе сети), `checkInternet()`.

### Документация: REF TSAR/REF АД и коррекция давления в TELO
Вопросы при тестировании Стандарт метода:

- **REF TSAR** (`rtsarM`) и **REF АД** (`adPressM`) в WEB мониторинге — это расчётные значения, зафиксированные **один раз** при входе в TELO (`rtsarM < 0.1`). Не меняются весь процесс, ни при залётах, ни при переходах NASEBYA → TELO. Обнуляются только при сбросе/начале новой колонны. Одинаковы для Стандарт и Шпора.
- **Коррекция давления** `(data.pressure - adPressM) * 0.0278` — применяется в обоих методах:
  - **Шпора**: учитывается для delta (снижение скорости) и hysteresis (залёт).
  - **Стандарт**: учитывается только для hysteresis (залёт → NASEBYA или finishTelo). Delta нет, так как в Стандарте нет механизма плавного снижения скорости — только бинарное решение "залёт / не залёт".
  - При росте давления (фронт с низким уходит): пороги повышаются → залёт позже. При падении: пороги снижаются → залёт раньше.

### ProcessEngine.cpp — Диагностика: лог cycleLim и bodyValveNC при старте TELO
`07d5cec`

- Добавлена печать обоих переменных в лог при старте ТЕЛО для диагностики бага (SPORA метод активируется при `cycleLim==3` вместо ожидаемого `cycleLim==1`).
- Формат: `TELO Start. Method: SPORA (cycleLim=3, bodyValveNC=true)`.

---

## 2026-04-03

### ProcessEngine.cpp — Пересчёт оставшегося времени ТЕЛО (Шпора)
`02372fa`

- Старая формула `(predictVol / speedShpora) - elapsedSec` завышала оставшееся время при каждом снижении скорости отбора, т.к. предполагала что текущая (сниженная) скорость была с самого начала.
- Новая формула `(predictVol - bodyVolDone) / speedShpora` правильно считает остаток объёма при текущей скорости. `bodyVolDone` аккумулируется на фактической скорости, поэтому расчёт всегда корректен.

### BuhloWar.ino, help.html — Обновление рабочих файлов
`4052e02`

- Обновлён `help.html` — переописан для новичков, простой язык, пошаговая инструкция.
- Мелкое исправление в `BuhloWar.ino`.

### index.html — Мелкое исправление
`712a886`

---

## 2026-03-31

### cloud/index.html — Калибровка из облачного интерфейса
`f8e81c2`, `704f23a`

- Добавлена поддержка команд калибровки клапанов через облачный режим (`cloud/index.html`).
- Исправлен формат `params` для команд калибровки — корректная передача параметров через облако.

### index.html — Восстановление блока ТЕЛО в мониторинге
`f3676ee`

- Восстановлены потерянные изменения блока ТЕЛО: объединены ПРОГНОЗ и ОТОБРАНО в единый формат `--/-- мл` по аналогии с блоком ГОЛОВЫ.
- Удалены дублирующие строки, заменены `bodyPredictEl`/`bodyVolEl` на `bodyTotalEl`/`bodyDoneEl`.

### AppNetwork.cpp — Удалены orphan реализации Telegram-методов
`72d7014`

- Удалены реализации `queueMessage`, `isTelegramReady`, `sendTelegramNow`, `processMessageQueue` — остались без объявлений в `.h` после чистки Telegram-зависимости (вызывали ошибки компиляции).

### CloudManager.h — Fix Kernel Panic от фрагментации кучи
`fa52998`

- `WiFiClientSecure` стал полем класса (`_client`) вместо локальной переменной в каждом методе.
- Ранее каждый вызов `sendTelemetry()`/`checkCommands()`/`checkSettings()` создавал новый TLS контекст (~20KB), вызывая фрагментацию кучи и Kernel Panic.
- Инициализация через `ensureClient()` — один раз при первом вызове.

### AppNetwork.h / AppNetwork.cpp — Удаление зависимости UniversalTelegramBot
`fa52998`

- `#include <UniversalTelegramBot.h>` закомментирован.
- Убраны: создание `bot`, загрузка `TELEGRAM_CERTIFICATE_ROOT` (~1.5KB RAM), очередь сообщений Telegram. Экономия ~20KB RAM.

### ValveCalMenu.h — Мастер калибровки: ввод объёма целыми числами
`fa52998`

- Шаг изменён с 0.1 на 1 (целые мл).
- `enteredVolume` инициализируется из текущего `cap` при переходе на шаг ввода (оператор редактирует, а не вводит с нуля).

### index.html — Мастер калибровки: целые числа + фикс сброса
`fa52998`

- Шаг +1/-1 мл вместо 0.1.
- Значение инициализируется из `cap` в настройках.
- Добавлен флаг `_lastInputStep` для защиты от сброса при обновлении статуса.

### ProcessEngine.cpp — NEXT_STAGE заблокирован на этапе VALVE_CAL
`b8faf64`

- Баг: блок `if (currentStage == VALVE_CAL) { return OK; }` стоял ДО обработчика `NEXT_STAGE` и глотал команду кнопки ДАЛЕЕ.
- Обработчик `NEXT_STAGE` перенесён перед блоком `VALVE_CAL`.
- Добавлен лог `[WebCmd] NEXT_STAGE received, currentStage=X` для диагностики.

### ProcessEngine.cpp — Команды калибровки из Web перехватывались
`362da00`

- Команды `CALIB_START_DRY`, `CALIB_START_CAPACITY`, `CALIB_CANCEL` перехватывались ранним `return` в блоке `VALVE_CAL`.
- Обработчики перенесены ДО проверки текущего этапа.

### index.html — Калибровка из Web + блок ТЕЛО мониторинг
`362da00`, `b49ec3a`

- Блок ТЕЛО: добавлен расчёт/факт (`--/-- мл`), убраны дублирующие ОТОБРАНО и ПРОГНОЗ.
- Мастер калибровки: fetch-запросы через `getCloudCommandUrl()` (работает через облако и локально с SD-карты).
- Автоматическое закрытие окна мастера через 2 сек после расчёта пропускной способности.
- Добавлена проверка TSA лимита с уведомлением и звуком (флаг `prevTsaAlarmState`).

---

## 2026-03-29

### index.html — Мониторинг: блок АД перемещён внутрь карточки с диаграммой
`05186ee`, `a7537a3`

- Давление (АД) перемещено из отдельного блока внутрь карточки с температурной диаграммой (справа от температур).
- Инфопанель: убрано давление из верхней панели (теперь в карточке), layout `grid-cols-5`.

### index.html — Мобильная адаптивность через CSS media queries
`fd4c34d`, `01db37f`

- Телефон (портрет): 1 колонка меню, компактные элементы.
- Телефон (ландшафт): компактный режим.
- Планшет: увеличенные элементы.
- ПК: широкий контейнер (1024px).
- Большой монитор: максимальная ширина (1280px).
- Исправлена адаптивность для портретной и ландшафтной ориентации.

### index.html — Мелкие фиксы веб-интерфейса
`8e6256f`, `fa464f2`

- Null guard для `pressure-value` (updateUI падал без элемента).
- Облако показывало «-1 сек назад» — исправлено через `Math.max(0, floor(...))`.

---

## 2026-03-28

### CloudManager.h — Оптимизация: ротация запросов (один за вызов)
`f83ecef`

- Максимальная блокировка loop снижена с 6 сек до 2 сек.
- Интервал проверки settings увеличен с 30 сек до 60 сек.
- Удалён неиспользуемый `ProcessConfigs.h` (дубликат структур).

### BuhloWar.ino — LCD обновление: интервал 500 мс
`595eb2d`

- Снижена нагрузка на I2C шину в 2.5 раза (было 200 мс). 2 обновления/сек достаточно для процесса.

---

## 2026-03-25

### help.html — Переописание для новичков
`e9b70f9`

- Полностью переписан `help.html` — простой язык, пошаговая инструкция для начинающих.

### Множество файлов — Fix GOLOVY_OK loop, LCD кнопки, cloud logs
`8cff9f5`

- Исправлен зацикленный GOLOVY_OK.
- Исправлены кнопки на LCD.
- Добавлены облачные логи (`cloud/api.php`, `AppNetwork.cpp`, `SDLogger.h`).

---

## 2026-03-24

### cloud/index.html, AppNetwork.cpp, menu_main.h — Звуковые уведомления
`584f1ea`

- Добавлен звуковой сигнал при срабатывании тревоги (W/A/X на LCD, safety_detail).

### Множество файлов — Оптимизация Шпора + первая крупная загрузка проекта
`e24cfc1`

- Оптимизация клапана Шпора: `minOpenTime`, снижение износа, мин. скорость 300 мл/ч.
- Расширенное логирование, GOLOVY_OK fix, LCD статус сети.
- Размер лога увеличен до 100KB.
- Загружены все основные файлы проекта: `ProcessEngine.cpp/.h`, `CloudManager.h`, `OutputManager.cpp/.h`, `SensorAdapter.cpp/.h`, `SensorManager.cpp/.h`, `ValveCalMenu.h`, `SetPwAsMenu.h`, `SDLogger.h`, `ProcessCommon.h`, `AppNetwork.h/.cpp`, `BuhloWar.ino`, `menu_*.h`, `config.h`, `preferences.cpp/.h`, `common.h`.
- Загружены файлы Android-приложения (`android/BuhloWarApp/`).
- Облачные скрипты (`cloud/api.php`, `cloud/index.html`).

---

## 2026-03-23

### cloud/index.html — Статус облака в баннере
`913e399`

- Статус подключения к облаку перемещён в баннер — всегда виден. Локальный режим отображается серым.

### CloudManager.h — Revert: WiFiClient reuse вызывал watchdog crash
`7073b01`

- Отменена попытка переиспользования WiFiClient (вызывала сброс watchdog).
- Вернуто создание нового клиента для каждого запроса.

### cloud/ — Оптимизация загрузки веб-интерфейса
`0bd9533`

- Убран цикл повторных попыток чтения с SD-карты, ускоряющий загрузку.
